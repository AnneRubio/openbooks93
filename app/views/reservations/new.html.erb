<div class="container">
  <div>
    <h1>Reservations#new</h1>
    <p>Find me in app/views/reservations/new.html.erb</p>
    <h2><%= @book.title %></h2>
    <p><%= @book.author %></p>
  </div>
  <div class="row">
  <h2>Book it Now 📓</h2>
  <%= simple_form_for [@book, @reservation], html: { data: { controller: 'flatpickr'} } do |f| %>
      <%= f.input :starting_date,
                  label: "Book from (dd/mm/yyyy)",
                  as: :string,
                  input_html: { data: { flatpickr_target: 'startDateInput' } } %>
      <%= f.input :ending_date,
                  label: "Until (dd/mm/yyyy)",
                  as: :string,
                  input_html: { data: { flatpickr_target: 'endDateInput'} } %>
      <%= f.button :submit, "Reserve", class:"btn btn-small" %>
    <% end %>
  </div>
</div>
